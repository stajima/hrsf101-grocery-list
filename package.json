{
  "name": "hrsf101-grocery-list",
  "version": "1.0.0",
  "description": "grocery list app",
  "main": "./server/server.js",
  "scripts": {
    "start": "nodemon ./server/server.js",
    "compile": "babel client/src/ --out-dir client/dist/compiled/ --presets=env,react --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "mysql": "^2.16.0"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
